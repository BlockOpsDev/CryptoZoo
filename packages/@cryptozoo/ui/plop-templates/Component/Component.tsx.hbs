import FragC from '../../utils/FragC';
import { gql } from '@apollo/client';

export interface {{pascalCase name}}Props {
  sample: any;
  //sample: {{pascalCase name}}_<gql_type>;
}

export const {{pascalCase name}}: FragC<{{pascalCase name}}Props> = ({sample}: {{pascalCase name}}Props) => {
  return <div/>
};

{{pascalCase name}}.fragments = {
  sample: gql`
    fragment {{pascalCase name}}_<gql_type> on <gql_type> {}
  `,
};